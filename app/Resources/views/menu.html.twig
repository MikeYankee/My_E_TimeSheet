<nav class="navbar navbar-custom navbar-fixed-top">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
            </button>
            <a class="navbar-brand" href="#"></i>&nbsp;MY E-TimeSheet</a>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                {% if is_granted("ROLE_DELEGUE") %}
                    {% include 'menu_delegue.html.twig' %}
                {% elseif is_granted("ROLE_ETUDIANT") %}
                    {% include 'menu_etudiant.html.twig' %}
                {% endif %}
                {% if is_granted("ROLE_RESPONSABLE") or is_granted("ROLE_SUPER_RESPONSABLE") %}
                    {% include 'menu_responsable.html.twig' %}
                {% elseif is_granted("ROLE_ENSEIGNANT") %}
                    {% include 'menu_enseignant.html.twig' %}
                {% endif %}
                {% if is_granted("ROLE_CFA") %}
                    {% include 'menu_cfa.html.twig' %}
                {% endif %}
                {% if is_granted("ROLE_SECRETAIRE") %}
                    {% include 'menu_secretaire.html.twig' %}
                {% endif %}
                {% if is_granted("ROLE_ADMIN") %}
                    {% include 'menu_admin.html.twig' %}
                {% endif %}


            </ul>

            <ul class="nav navbar-nav navbar-right">
                <li><a href="#" class="disabled">Profil&nbsp;<i class="glyphicon glyphicon-user"></i></a></li>
                <li><a href="#" class="disabled">Contact&nbsp;<i class="glyphicon glyphicon-envelope"></i></a></li>
                <li><a href="{{ path('deconnexion') }}" >Déconnexion&nbsp;<i class="glyphicon glyphicon-off"></i></a></li>
            </ul>
        </div>
    </div>
</nav>

<script>
    $(document).ready(function() {
        $('a[href="' + this.location.pathname + '"]').parent().addClass('active');
    });
</script>